{
  "name": "@flags/auth-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nitro build",
    "clean": "git clean -xdf .cache .nitro .output .turbo .vercel node_modules",
    "lint": "eslint",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@auth/core": "0.34.2"
  },
  "devDependencies": {
    "@flags/eslint-config": "workspace:*",
    "@flags/prettier-config": "workspace:*",
    "@flags/tailwind-config": "workspace:*",
    "@flags/tsconfig": "workspace:*",
    "@types/node": "^20.16.11",
    "eslint": "catalog:",
    "h3": "^1.13.0",
    "nitropack": "^2.9.7",
    "prettier": "catalog:",
    "typescript": "catalog:"
  },
  "prettier": "@flags/prettier-config"
}
